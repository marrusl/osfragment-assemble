[fragment]
name = "grafana"
version = "11.0"
description = "Grafana monitoring dashboard — repo, package, and service enablement"
vendor = "Grafana Labs"

[fragment.provides]
repos = ["grafana"]

[fragment.packages]
required = ["grafana"]
