[fragment]
name = "node-exporter"
version = "1.8.0"
description = "Prometheus Node Exporter: self-contained with EPEL repo"

[fragment.provides]
repos = ["epel"]

[fragment.packages]
required = ["node-exporter"]
